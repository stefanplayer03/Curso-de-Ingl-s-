[build]
  command = "npm run build"
  publish = "dist"

# Necessário para SPA com React Router: qualquer rota cai no index.html
# e o roteamento do lado do cliente assume a partir daí.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
